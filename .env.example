# Environment Configuration for Fitness Tracker
# Copy this file to .env and fill in your values

# Application Settings
NODE_ENV=production
PORT=3000

# JWT Secret (REQUIRED - Generate a secure random string)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Database Options (The app will try in this order: MySQL -> PostgreSQL -> SQLite)

# Option 1: MySQL Database
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=fitness_tracker

# Option 2: PostgreSQL Database (used by Railway, Render, Heroku)
# DATABASE_URL=postgresql://username:password@hostname:port/database

# Option 3: SQLite (default fallback - no configuration needed)
# Will create fitness_tracker.db file automatically

# Frontend URL (for CORS in production)
FRONTEND_URL=https://your-domain.com

# Examples for popular hosting services:

# Railway PostgreSQL:
# DATABASE_URL=${{DATABASE_URL}} (Railway provides this automatically)

# Render PostgreSQL:
# DATABASE_URL=postgresql://user:pass@hostname:port/dbname

# PlanetScale MySQL:
# DB_HOST=your-host.planetscale.dev
# DB_USER=your-username
# DB_PASSWORD=your-password
# DB_NAME=your-database

# AWS RDS:
# DB_HOST=your-rds-endpoint.amazonaws.com
# DB_USER=admin
# DB_PASSWORD=your-password
# DB_NAME=fitness_tracker
